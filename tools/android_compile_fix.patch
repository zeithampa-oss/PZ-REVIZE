--- a/source/android/app/src/main/java/cz/pzrevize/mobile/MainActivity.java
+++ b/source/android/app/src/main/java/cz/pzrevize/mobile/MainActivity.java
@@ -978,7 +978,7 @@
         detail.addView(text(chosenTitle,21,C_TEXT,true),marginBottom(dp(6)));detail.addView(text(rowTypeLabel(measurementRowType(chosen))+"  •  "+nz(chosen.get("measure_type")),13,C_MUTED,false),marginBottom(dp(12)));
         LinearLayout values=measurementValueGrid(chosen);if(values.getChildCount()>0)detail.addView(values,marginBottom(dp(10)));if(!chosen.get("note").isEmpty())detail.addView(text(chosen.get("note"),14,C_MUTED,false),marginBottom(dp(10)));
         detail.addView(smallButton("UPRAVIT VYBRANOU POLOŽKU",C_ORANGE,C_TEXT,v->measurementDialog(chosen)),marginBottom(dp(12)));
-        Db.Row owner=chosen;if(!isTreeParent(measurementRowType(chosen))){String pk=nz(chosen.get("parent_key"));if(!pk.isEmpty())for(Db.Row r:rows)if(pk.equals(nz(r.get("item_key")))){owner=r;break;}else{for(Db.Row r:roots){List<Db.Row> kids=children.get(nodeKey(r));if(kids!=null&&kids.contains(chosen)){owner=r;break;}}}}
+        Db.Row owner=chosen;if(!isTreeParent(measurementRowType(chosen))){String pk=nz(chosen.get("parent_key"));if(!pk.isEmpty())for(Db.Row candidate:rows)if(pk.equals(nz(candidate.get("item_key")))){owner=candidate;break;}else{for(Db.Row rootCandidate:roots){List<Db.Row> kids=children.get(nodeKey(rootCandidate));if(kids!=null&&kids.contains(chosen)){owner=rootCandidate;break;}}}}
         if(isTreeParent(measurementRowType(owner))){final Db.Row parent=owner;detail.addView(text("Přidat k: "+(parent.get("element").isEmpty()?"prvku":parent.get("element")),13,C_MUTED,true),marginBottom(dp(7)));String[] labels={"BOD","SPOJITOST","RCD / RCBO","POZNÁMKA"};String[] types={"POINT","CONTINUITY","RCD","NOTE"};for(int row=0;row<2;row++){LinearLayout a=new LinearLayout(this);a.setOrientation(LinearLayout.HORIZONTAL);for(int col=0;col<2;col++){int i=row*2+col;final String t=types[i];LinearLayout.LayoutParams lp=new LinearLayout.LayoutParams(0,dp(46),1);if(col>0)lp.leftMargin=dp(6);a.addView(smallButton("＋ "+labels[i],C_BG,C_TEXT,v->measurementDialog(null,t,parent)),lp);}detail.addView(a,marginBottom(dp(6)));}}
         detail.addView(smallButton("SMAZAT VYBRANOU POLOŽKU",Color.WHITE,C_RED,v->new AlertDialog.Builder(this).setTitle("Smazat položku?").setMessage("Smazat vybranou položku měření?").setNegativeButton("Ne",null).setPositiveButton("Ano",(d,w)->{db.deleteMeasurement(chosen.id());selectedMeasurementId=0;showMeasurements(false);}).show()),marginTop(dp(10)));
     }
